package diag.reiter;

/**
 * A conflict is an object that rejects some scenarios; 
 * this rejection usually means that the scenario 
 * cannot be an explanation of the observations.  
 * However, a conflict is usually defined to reject a set of scenarios, 
 * and this set of scenarios is usually defined as the set of scenarios 
 * represented by a set of hypotheses.  
 * */
public interface Conflict {

}
